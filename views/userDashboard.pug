doctype html
html
  head
    title Sign Up
    meta(charset="utf-8")
  body
    h1 Hola usuario

    // Botón para ir al Dashboard
    form(action="/dashboard" method="get")
      button(type="submit") Ir al Dashboard

    br

    // Botón para cerrar sesión
    form(action="/logout" method="get")
      button(type="submit") Cerrar sesión